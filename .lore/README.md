# Lore Project Memory

This repository uses Lore to manage its own requirements, features, stories, ADRs and tests.

Useful commands:

```bash
lore search skill
lore show FEATURE-001 --recursive
lore validate
```
